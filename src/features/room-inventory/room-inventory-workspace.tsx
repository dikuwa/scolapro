"use client";
import {useActionState,useEffect,useMemo,useState} from "react";
import {toast} from "sonner";
import {Button} from "@/components/ui/button";
import {assignCustodian,changeItem,createItem,verifyInventory,type RoomInventoryActionState} from "@/features/room-inventory/server/actions";
import type {RoomInventoryItem,RoomInventoryRoom,RoomInventoryStaff,RoomInventoryVerification} from "@/features/room-inventory/server/queries";
const init:RoomInventoryActionState={}; const f="mt-1 min-h-10 w-full rounded-[var(--radius-sm)] border border-border-subtle bg-surface-elevated px-3 text-sm";
function useNotice(s:RoomInventoryActionState){useEffect(()=>{if(s.message)s.success?toast.success(s.message):toast.error(s.message)},[s]);}
export function RoomInventoryWorkspace({rooms,items,staff,verifications,today,canAssign}:{rooms:RoomInventoryRoom[];items:RoomInventoryItem[];staff:RoomInventoryStaff[];verifications:RoomInventoryVerification[];today:string;canAssign:boolean}){const[roomId,setRoomId]=useState(rooms[0]?.id??"");const[ownership,setOwnership]=useState("");const[condition,setCondition]=useState("");const[q,setQ]=useState("");const room=rooms.find(r=>r.id===roomId);const visible=useMemo(()=>items.filter(i=>(!roomId||i.roomId===roomId)&&(!ownership||i.ownership===ownership)&&(!condition||i.condition===condition)&&(!q||i.name.toLowerCase().includes(q.toLowerCase())||(i.assetNumber??"").toLowerCase().includes(q.toLowerCase()))),[items,roomId,ownership,condition,q]);const[a,assign,p1]=useActionState(assignCustodian,init);const[c,create,p2]=useActionState(createItem,init);const[ch,change,p3]=useActionState(changeItem,init);const[v,verify,p4]=useActionState(verifyInventory,init);useNotice(a);useNotice(c);useNotice(ch);useNotice(v);return <div className="space-y-5"><section className="rounded-[var(--radius-md)] border border-border-subtle bg-surface p-4 shadow-[var(--shadow-xs)]"><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-5"><label><span className="text-xs text-muted-foreground">Room</span><select className={f} value={roomId} onChange={e=>setRoomId(e.target.value)}>{rooms.map(r=><option key={r.id} value={r.id}>{r.block?`${r.block} · `:""}{r.code} · {r.name}</option>)}</select></label><label><span className="text-xs text-muted-foreground">Ownership</span><select className={f} value={ownership} onChange={e=>setOwnership(e.target.value)}><option value="">All</option><option value="government">GRN / Government</option><option value="school">School</option><option value="personal">Personal</option></select></label><label><span className="text-xs text-muted-foreground">Condition</span><select className={f} value={condition} onChange={e=>setCondition(e.target.value)}><option value="">All</option>{["new","good","fair","poor","damaged","lost","disposed"].map(x=><option key={x}>{x}</option>)}</select></label><label className="lg:col-span-2"><span className="text-xs text-muted-foreground">Search item / asset no.</span><input className={f} value={q} onChange={e=>setQ(e.target.value)} placeholder="Search inventory"/></label></div></section>{room?<><div className="grid gap-4 lg:grid-cols-3"><section className="rounded-[var(--radius-md)] border border-border-subtle bg-surface p-4"><p className="text-xs text-muted-foreground">Selected room</p><h2 className="mt-1 font-semibold">{room.code} · {room.name}</h2><p className="mt-2 text-sm text-muted-foreground">{room.block||"No building/section"} · {room.itemCount} item lines</p><p className="mt-1 text-sm">Responsible: {room.custodianName||"Not assigned"}</p><p className="mt-1 text-xs text-muted-foreground">Last verified: {room.lastVerified||"Never"}</p></section>{canAssign?<form action={assign} className="rounded-[var(--radius-md)] border border-border-subtle bg-surface p-4"><input type="hidden" name="roomId" value={room.id}/><h3 className="scolapro-section-title">Responsible staff</h3><select className={f} name="staffId" required defaultValue={room.custodianId??""}><option value="">Choose staff</option>{staff.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select><input className={f} name="effectiveFrom" type="date" defaultValue={today}/><div className="mt-3 flex justify-start"><Button loading={p1} disabled={p1}>Assign custodian</Button></div></form>:null}<form action={verify} className="rounded-[var(--radius-md)] border border-border-subtle bg-surface p-4"><input type="hidden" name="roomId" value={room.id}/><h3 className="scolapro-section-title">Verify inventory</h3><select className={f} name="status" defaultValue="confirmed"><option value="confirmed">No changes — confirm inventory</option><option value="exceptions_noted">Exceptions noted</option></select><input className={f} name="notes" placeholder="Optional verification note"/><div className="mt-3 flex justify-start"><Button loading={p4} disabled={p4}>Confirm inventory</Button></div></form></div><form action={create} className="rounded-[var(--radius-md)] border border-border-subtle bg-surface p-4 sm:p-5"><input type="hidden" name="roomId" value={room.id}/><h2 className="scolapro-section-title">Add inventory item</h2><div className="mt-3 grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><input className={f} name="name" placeholder="Item description" required/><select className={f} name="ownership" defaultValue="government"><option value="government">GRN / Government</option><option value="school">School-owned</option><option value="personal">Personal</option></select><input className={f} name="quantity" type="number" min="0" defaultValue="1"/><select className={f} name="condition" defaultValue="good">{["new","good","fair","poor","damaged","lost","disposed"].map(x=><option key={x}>{x}</option>)}</select><input className={f} name="assetNumber" placeholder="Asset / GRN no. (optional)"/><input className={`${f} lg:col-span-2`} name="notes" placeholder="Notes / location"/></div><div className="mt-3 flex justify-start sm:justify-end"><Button loading={p2} disabled={p2}>Add item</Button></div></form><section className="rounded-[var(--radius-md)] border border-border-subtle bg-surface p-4 sm:p-5"><h2 className="scolapro-section-title">Current inventory</h2>{visible.length?<div className="mt-3 space-y-3">{visible.map(i=><form action={change} key={i.id} className="grid gap-2 rounded-[var(--radius-sm)] bg-surface-muted p-3 lg:grid-cols-[2fr_1fr_1fr_1fr_1fr_auto]"><input type="hidden" name="itemId" value={i.id}/><div><p className="text-sm font-medium">{i.name}</p><p className="text-xs text-muted-foreground">{i.assetNumber||"No asset no."} · {i.ownership} · qty {i.quantity} · {i.condition}</p></div><select className={f} name="eventType" defaultValue="correction"><option value="quantity_increase">Quantity increase</option><option value="quantity_decrease">Quantity decrease</option><option value="damaged">Damaged</option><option value="lost">Lost</option><option value="disposed">Disposed</option><option value="transferred_out">Transferred out</option><option value="correction">Correction</option><option value="ownership_correction">Ownership correction</option></select><input className={f} name="delta" type="number" defaultValue="0"/><select className={f} name="condition" defaultValue=""><option value="">Keep condition</option>{["new","good","fair","poor","damaged","lost","disposed"].map(x=><option key={x}>{x}</option>)}</select><select className={f} name="ownership" defaultValue=""><option value="">Keep ownership</option><option value="government">GRN</option><option value="school">School</option><option value="personal">Personal</option></select><Button loading={p3} disabled={p3}>Record</Button></form>)}</div>:<p className="mt-3 text-sm text-muted-foreground">No inventory matches the current filters.</p>}</section><section className="rounded-[var(--radius-md)] border border-border-subtle bg-surface p-4"><h2 className="scolapro-section-title">Verification history</h2><div className="mt-2 space-y-2">{verifications.filter(x=>x.roomId===room.id).slice(0,8).map(x=><div key={x.id} className="flex justify-between gap-3 border-t border-border-subtle py-2 text-sm"><span>{x.verifiedOn} · {x.status.replaceAll("_"," ")}</span><span className="text-muted-foreground">{x.itemCount} items</span></div>)}</div></section></>:<p className="rounded-[var(--radius-md)] bg-surface p-5 text-sm text-muted-foreground">No rooms are available for your current inventory scope.</p>}</div>}
